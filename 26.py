# String Length Finder using python programming language 

# Get a string from the user
text = input("Enter a string: ")

# Find the length of the string
length = len(text)

# Display the result
print("Length of the string is:", length)
